<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:mcp="http://www.mulesoft.org/schema/mule/mcp"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/mcp http://www.mulesoft.org/schema/mule/mcp/current/mule-mcp.xsd">

    <!-- ==========================================
         CONFIGURATION PROPERTIES
         ========================================== -->
    <configuration-properties
        file="config.yaml"
        doc:name="Config Properties"
        doc:description="Loads application configuration from config.yaml (HTTP port, OS settings, logging)" />

    <!-- ==========================================
         HTTP LISTENER CONFIGURATION
         ========================================== -->
    <http:listener-config
        name="HTTP_Listener_config"
        doc:name="HTTP Listener Config"
        doc:description="Inbound HTTP listener for the Opportunities System API on port 8081">
        <http:listener-connection
            host="${http.host}"
            port="${http.port}" />
    </http:listener-config>

    <!-- ==========================================
         MCP SERVER CONFIGURATION
         Exposes the same opportunity flows as MCP tools over
         Streamable HTTP, mounted on the same listener/port as the
         REST API at /mcp.
         ========================================== -->
    <mcp:server-config
        name="MCP_Server_Config"
        doc:name="MCP Server Config"
        serverName="Opportunities MCP Server"
        serverVersion="1.0.0">
        <mcp:streamable-http-server-connection
            listenerConfig="HTTP_Listener_config"
            mcpEndpointPath="/mcp" />
    </mcp:server-config>

    <!-- ==========================================
         OBJECT STORE — IN-MEMORY OPPORTUNITY STORE
         ========================================== -->
    <os:object-store
        name="Opportunities_Object_Store"
        persistent="false"
        maxEntries="500"
        doc:name="Opportunities Object Store"
        doc:description="In-memory non-persistent Object Store keyed by oppId; seeded at startup from seed-opportunities.json" />

    <!-- ==========================================
         GLOBAL ERROR HANDLER
         ========================================== -->
    <error-handler name="Global_Error_Handler"
        doc:name="Global Error Handler"
        doc:description="Catches any unhandled application error and returns a structured JSON error response">
        <on-error-propagate type="ANY"
            doc:name="Propagate Any Error"
            doc:description="Catches all unhandled errors and serializes them as a JSON error response with HTTP 500">
            <ee:transform doc:name="Build Error Response" doc:description="Transforms the Mule error into a structured JSON error payload with status, message, correlationId, and timestamp">
                <ee:message>
                    <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    status: 500,
    message: error.description default "An unexpected error occurred",
    correlationId: correlationId,
    timestamp: now() as String {format: "yyyy-MM-dd'T'HH:mm:ss.SSSZ"}
}]]></ee:set-payload>
                </ee:message>
                <ee:variables>
                    <ee:set-variable variableName="httpStatus"><![CDATA[%dw 2.0
output application/java
---
500]]></ee:set-variable>
                </ee:variables>
            </ee:transform>
            <logger
                level="ERROR"
                message="#['[' ++ correlationId ++ '] Unhandled error: ' ++ (error.description default 'unknown')]"
                doc:name="Log Unhandled Error"
                doc:description="Logs the full error description with correlation ID for observability" />
        </on-error-propagate>
    </error-handler>

</mule>